# CLAUDE.md — АСМР, модуль расписания (прототип)

## Что это

Интерактивный прототип нового модуля расписания для **АСМР** — внутренней
CRM/LMS образовательной компании WeGlobal (Казахстан, подготовка к IELTS, SAT,
NUET, ЕНТ).

Прототип нужен, чтобы разработчик и академический директор поняли логику модуля
до начала реальной разработки. Это **не продакшн, но и не статичный мокап**:
создал группу — появились занятия, отметил посещаемость — урок посчитался в
зарплату.

Бэкенда и базы нет. Данные — стартовый seed в JSON, дальше всё в localStorage.

Живая версия: https://yernurby.github.io/asmr-schedule-prototype/

---

## Текущий шаг

**Часть 3 — календари, доступность и нагрузка. Завершена.**
ТЗ: `docs/03-календари-и-нагрузка.md`.

| № | Часть | Статус |
|---|---|---|
| 0 | Каркас | готов |
| 1 | Предметы у курсов | готова |
| 2 | Занятия и изменение расписания | готова |
| 3 | Календари, доступность и нагрузка | **готова** |
| 4 | Посещаемость | не начата |
| 5 | Замены и переносы | не начата |
| 6 | Зарплата по факту занятий | не начата |

Что дала часть 1:

- сущность `Subject` — предмет принадлежит одному курсу и не пересекается с другими;
- строка расписания несёт предмет, преподавателя и ссылку на Meet;
- `Group.teacherIds` **удалён** — список преподавателей считается из расписания
  функцией `groupTeacherIds()`;
- у преподавателя появилось поле `subjectIds`.

Что дала часть 2:

- сущность `Lesson` — занятие с датой, состоянием и списком групп;
- `originalTeacherId` — кто был назначен изначально; расхождение с `teacherId`
  и есть «замена», по которой части 2 нельзя ничего удалять молча;
- общие занятия на несколько групп (`sourceRowId === null`) с сериями (`seriesId`);
- `AuditEntry` — журнал действий; `frozenMonths` — закрытые по зарплате месяцы.

Что дала часть 3:

- `Availability` — недельный шаблон преподавателя в 30-минутных ячейках
  `"<день>-<ЧЧ:ММ>"`, без дат. Ничего не запрещает (§4);
- `ReshuffleRequest` — просьба снять слот, вместо сообщения в чате;
- раздел «Расписание» с вкладками — календарь, список занятий, нагрузка,
  вне доступности. Пункт «Занятия» из левого меню убран (§32).

---

## Порядок работы над каждой частью

Заказчик присылает текст ТЗ по одной части за раз. На каждую часть:

1. Сохранить присланный текст в `docs/` отдельным файлом: `01-…md`, `02-…md`, …
   Он сразу становится виден в прототипе на странице «Требования» — она читает
   всю папку `docs/` через `import.meta.glob`.
2. Прочитать ТЗ целиком и реализовать точно по нему.
3. Если ТЗ противоречит уже написанному коду или непонятно — **не додумывать**,
   а спросить заказчика.
4. Обновить карту модуля в `src/data/moduleMap.ts`: `status`, `screens`,
   `whatToLook`. Обновить таблицу статусов в этом файле и в `README.md`.
5. Поднять `APP_VERSION` и `APP_STEP` в `src/version.ts`.
6. Закоммитить, запушить, дать заказчику ссылку.

---

## Стек

| Слой | Технология |
|---|---|
| Сборка | Vite 6 |
| UI | React 19 + TypeScript (strict) |
| CSS | Tailwind CSS 3.4 |
| Роутинг | react-router-dom 7, **HashRouter** |
| Состояние | Zustand 5 + `persist` в localStorage |
| Markdown | marked (страница «Требования») |
| Публикация | GitHub Actions → GitHub Pages |

**Tailwind должен оставаться на 3.x.** В версии 4 стоковая палитра переехала на
OKLCH, и точные HEX-значения из `docs/design-tokens.md` перестанут совпадать
с реальной системой.

---

## Дизайн — жёсткое правило

Задача не «сделать похоже», а **точно воспроизвести визуальный язык АСМР**.
Разработчик, который будет это делать, должен узнавать свою систему.

- Все цвета, шрифты, радиусы, размеры и отступы берутся **только** из
  [`docs/design-tokens.md`](docs/design-tokens.md). Значения там сняты пиксельно
  с 39 скриншотов реальной системы, а не подобраны на глаз.
- Реальная АСМР построена на **стоковой палитре Tailwind** (`slate` как
  нейтральная, `emerald` / `amber` / `red` как акценты) — значения совпали до
  байта. Поэтому кастомных цветов в `tailwind.config.js` нет, только семантические
  алиасы (`page`, `surface`, `muted`, `line`, `line-strong`, `line-input`).
- Радиусов в системе ровно три: **6px** (карточки, кнопки, поля, вкладки),
  **8px** (модалка), **full** (пилюли). Других не вводить.
- Новый цвет, шрифт или радиус — **только после вопроса заказчику**. Если чего-то
  нет в скриншотах, это перечислено в разделе «Чего в скриншотах нет» в
  `design-tokens.md`.
- Опись существующих экранов — [`docs/existing-screens.md`](docs/existing-screens.md),
  сами скриншоты — `reference/screens/` (`_filenames.txt` хранит соответствие
  исходным именам файлов).

**Интерфейс полностью на русском. Код и комментарии — на английском.**

---

## Структура

```
docs/                     ТЗ и рабочие документы (видны в прототипе)
reference/screens/        39 скриншотов реальной АСМР
scripts/generate-seed.mjs Генератор стартовых данных
src/
  app/
    navigation.tsx        Меню по ролям + домашний маршрут роли
    router.tsx            Маршруты
    layout/               AppLayout, Sidebar, Topbar, RoleSwitcher, TimeMachine
  data/
    types.ts              Доменные типы
    seed.json             Стартовые данные (генерируются, коммитятся)
    moduleMap.ts          Карта модуля — источник правды по статусам частей
  lib/                    date.ts, format.ts, scope.ts, subjects.ts, lessons.ts, people.ts
  pages/                  Экраны
  store/
    useDataStore.ts       Доменные данные, persist
    useSessionStore.ts    Роль, актор, время прототипа
  ui/                     Примитивы: Button, Card, Table, Pill, Tabs, Modal, Field, …
  version.ts              Номер версии и подпись шага
```

---

## Соглашения

### Время

Всё приложение считает «настоящим» время из `useSessionStore` (`today`, `time`),
которое задаётся панелью времени в шапке. **Нигде не вызывать `new Date()`
напрямую** — иначе панель времени перестанет работать, а она нужна части 4,
чтобы показывать открытие и закрытие окна отметки посещаемости.

Для арифметики есть `sessionNow(state)` и хелперы в `src/lib/date.ts`.

### Роли

Четыре роли: `academ_head` (академический директор), `teacher`, `curator`,
`student`. Для преподавателя, куратора и студента в шапке выбирается конкретный
человек (`actorId`) — от него зависит, какие данные видно.

Меню роли задаётся в `src/app/navigation.tsx`. Видимость данных — через
`scopeGroups()` в `src/lib/scope.ts`.

У преподавателя с части 2 есть «Мои занятия» — только его уроки, без создания
общих занятий и без массовой отмены (§11). У студента по-прежнему пусто, до части 4.

### Данные

- `scripts/generate-seed.mjs` → `src/data/seed.json`. Генератор **детерминированный**:
  никаких `Math.random` и `Date.now`, повторный запуск даёт тот же файл.
- Меняешь форму или содержимое данных — подними `SEED_VERSION` в генераторе.
  `useDataStore` сравнивает версию и перезаливает localStorage.
- Новая сущность части модуля (предметы, занятия, посещаемость, замены) добавляется
  слайсом в `useDataStore` и полем в `SeedData`.

### Предметы (часть 1)

Инварианты живут в `src/lib/subjects.ts`, а не размазаны по экранам:

- `courseSubjects()` — живые предметы курса; `allCourseSubjects()` — вместе с архивными;
- `isSingleSubject()` — курс с одним предметом. Такие курсы обязаны выглядеть
  ровно как до части 1: без группировки в форме и без названия предмета в списке;
- `deleteBlockedBecause()` / `archiveBlockedBecause()` — почему нельзя удалить или
  заархивировать. Экраны только показывают причину, решение принимает библиотека;
- `groupTeacherIds()` — **единственный** способ узнать преподавателей группы;
- `teachersForSubject()` — кого предлагать первым при постановке в расписание.

Архивный предмет, за которым осталось расписание, продолжает показываться в форме
группы и в карточке — иначе его строки стали бы недоступны.

### Занятия (часть 2)

Инварианты в `src/lib/lessons.ts`:

- `generateGroupLessons()` — единственный способ создать занятия из расписания;
- `planScheduleChange()` — считает предпросмотр (что удалить, что создать, что
  затронуто заменами), но **ничего не меняет**. Применяет только store;
- `hasSubstitution()` — определение «занятия с заменой» до появления части 5:
  `teacherId !== originalTeacherId`;
- `isMonthFrozen()` — §24, блокировка даты внутри закрытого месяца;
- `findConflicts()` — §13, занятость преподавателей и групп;
- `migrateSchedule()` — §31–§33, чистая функция: возвращает поправленные группы
  и список «требуют разбора», ничего не записывая.

Правила, которые легко нарушить:

- изменение расписания существующей группы **всегда** идёт через
  `ScheduleChangeModal`. Прямой вызов `saveGroupSchedule` минует дату вступления
  в силу, предпросмотр и журнал;
- у новой группы занятия создаются сразу и без даты — сохранять нечего;
- отменённые занятия не удаляются, а получают `state: 'cancelled'` и причину:
  §20 требует, чтобы они не шли в зарплату и не считались пропусками, а §21 —
  чтобы их можно было вернуть;
- состояния `held` и `unmarked` не выставляет никто: они появятся в части 4.

### Календари (часть 3)

- `src/ui/WeekGrid.tsx` — единственная недельная сетка на все три календаря.
  Шаг 30 минут, `assignLanes()` раскладывает наложившиеся занятия по дорожкам
  (§12). Сетка же используется как редактор доступности: те же ячейки принимают
  `onCellMouseDown` / `onCellMouseEnter`;
- `src/lib/availability.ts` — ячейки, `isOutsideAvailability()` (§5),
  `outsideAvailabilitySlots()` (группирует по слоту недели, а не по датам, иначе
  список нечитаем), `findWindows()` и `buildVariants()` (§24);
- `src/lib/calendar.ts` — `weekDays()`, цвета блоков по состоянию (§14), легенда.

Осторожно: у преподавателя без размеченной доступности **нет** конфликтов —
отсутствие заявления не считается расхождением. Иначе список сразу забился бы
всеми, кто ничего не отмечал.

Доступность — шаблон на неделю, а занятия датированные. Поэтому «занят» в подборе
окна считается на горизонте четырёх недель вперёд, а не «вообще».

### Экраны модуля

На каждом экране, который относится к модулю расписания, ставится плашка
`<PartBadge part={N} />` — маленькая серая, вида «Часть 2 · Занятия и изменение
расписания». Экраны «Курсы», «Группы», карточка группы и «Сотрудники» несут
плашку части 1, потому что часть 1 их меняет.

### Публикация

- `vite.config.ts` → `base: '/asmr-schedule-prototype/'`. Без этого на Pages не
  подхватятся стили.
- `HashRouter`, чтобы прямые ссылки на страницы работали.
- Пуш в `main` → workflow `.github/workflows/deploy.yml` собирает и публикует.
  Ссылка одна и та же всегда.

---

## Стартовые данные

Опорная дата прототипа — **17.08.2026**. Вокруг неё разложены группы:

- **Идут сейчас:** NUET 1.1, NUET 1.2, NUET 1.3, IELTS INTENSIVE 7, SAT 7
- **Стартуют через две недели (31.08.2026):** NUET 1.4, NUET 1.5, IELTS 64
- **Завершилась:** IELTS MAY

5 курсов (IELTS, SAT, NUET, Pre-IELTS (RTN), Pre-IELTS (VIP)), 7 предметов,
9 групп, 7 преподавателей, 4 куратора, 1 академический директор, 177 студентов.

У Pre-IELTS (RTN) и Pre-IELTS (VIP) групп пока нет — курсы заведены, чтобы
справочник выглядел как настоящий.

**Предметы в seed.** Миграция из части 1 (§17) даёт каждому курсу один предмет,
названный как курс. Seed изображает состояние *после* того, как академический
директор доделал §18 руками: у SAT — Math и Verbal, у NUET — Математика и
Критическое мышление, с разными преподавателями и разными днями. IELTS и оба
Pre-IELTS оставлены с одним предметом намеренно — на них видно, что для
односоставных курсов ничего не изменилось (§11, §14).

У NUET 1.5 по «Критическому мышлению» преподаватель не назначен — это состояние
«не назначен» и проверка того, что расчётное поле «Преподаватели» просто не
включает пустой слот.
